/*
Creates gui for scaling data received from an osc message followed by an array of numeric values. Creates a single horizontal widget array for setting these:

[envir, param, index, frommin, frommax, tomin, tomax]

Also creates a bus stored in 


Can be used as an element in creating a window screen composed of several widgets, with symbol.v. 

*/

OSCDispatcherGUI {
	classvar <busses;

	
	
}